import { suite, test, slow, timeout, skip, only } from "mocha-typescript";
import { expect } from "chai";

@suite("Pipeline tests")
class PipelineTest
{
    @test("Should run the piped stages")
    assertTest() {
        
    }
}